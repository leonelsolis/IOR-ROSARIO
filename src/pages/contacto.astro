---
import BaseLayout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---

<style>
  .contact-container {
    max-width: 800px;
    margin: 4rem auto;
    padding: 2rem;
  }

  .contact-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .contact-header h1 {
    font-size: 2.5rem;
    color: #2c2c2c;
    margin-bottom: 1rem;
    font-weight: 700;
  }

  .contact-header p {
    font-size: 1.1rem;
    color: #666;
    line-height: 1.6;
  }

  .form {
    background: white;
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form input,
  .form textarea {
    padding: 0.875rem 1rem;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    font-family: inherit;
    font-size: 1rem;
    transition: all 0.3s ease;
    background: #fafafa;
  }

  .form input:focus,
  .form textarea:focus {
    outline: none;
    border-color: #667eea;
    background: white;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
  }

  .form textarea {
    resize: vertical;
    min-height: 140px;
    font-size: 0.95rem;
  }

  .form button {
    padding: 1rem;
    background: #4a4a4a;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .form button:hover {
    background: #2c2c2c;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
  }

  .contact-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
    padding-top: 3rem;
    border-top: 2px solid #f0f0f0;
  }

  .info-item {
    text-align: center;
  }

  .info-item h3 {
    color: #667eea;
    margin-bottom: 0.5rem;
    font-size: 1rem;
  }

  .info-item p {
    color: #666;
    font-size: 0.95rem;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .contact-container {
      margin: 2rem auto;
      padding: 1rem;
    }

    .contact-header h1 {
      font-size: 2rem;
    }

    .form {
      padding: 1.5rem;
    }
  }
</style>

<BaseLayout title="Contacto - Instituto Oncológico">
  <Header slot="header" />

  <div class="contact-container">
    <div class="contact-header">
      <h1>Contacto</h1>
      <p>Complete el siguiente formulario para solicitar un turno o hacer una consulta. Nos pondremos en contacto lo antes posible.</p>
    </div>

    <form class="form" action="#" method="POST">
      <div class="form-group">
        <input type="text" placeholder="Nombre completo" required />
      </div>
      <div class="form-group">
        <input type="email" placeholder="Correo electrónico" required />
      </div>
      <div class="form-group">
        <input type="text" placeholder="Teléfono" required />
      </div>
      <div class="form-group">
        <textarea placeholder="Escriba su mensaje" required></textarea>
      </div>
      <button type="submit">Enviar Consulta</button>
    </form>

    <div class="contact-info">
      <div class="info-item">
        <h3>Teléfono</h3>
        <p>+54 (341) 123-4567</p>
      </div>
      <div class="info-item">
        <h3>Email</h3>
        <p>contacto@institutodeoncologia.com</p>
      </div>
      <div class="info-item">
        <h3>Ubicación</h3>
        <p>Rosario, Argentina</p>
      </div>
    </div>
  </div>

  <Footer slot="footer" />
</BaseLayout>
